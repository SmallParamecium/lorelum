# benchmark-pack（测试夹具）

本目录是 `lore query / get` 的检索测试夹具包。

**来源**：practice 正文取自 `lorelum/lorelum-benchmark` 的
`incubator/practice-injection/*/private/practices/*.md`（2026-08-11 提取）。
其中 6 份为原样保留（`react.*` 结构化 Practice）；
`frontend.layered-design`、`llm.provider-gateway`、`api.pagination-convention`
原为仓库"约定"文档，为适配测试补写了 frontmatter（正文未改）。

**用途**：仅作检索/排序/无结果/全文的确定性测试数据，不代表正式知识包。
